# pubspec.yaml (GAMERZ ADDA Root Folder mein)

name: gamerz_adda
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # ZAROORI PACKAGES (Video Player aur Payment)
  http: ^1.1.0
  video_player: ^2.8.1
  razorpay_flutter: ^1.3.0
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# --- YAHAN PAR ASSETS DECLARATION AAYEGA ---
flutter:
  uses-material-design: true

  # SAARI IMAGES AUR VIDEO YAHAN DECLARE HONGE
  assets:
    # Aam taur par, poora folder declare kar dete hain
    - lib/assets/ 
    
    # Lekin zyada safai ke liye, hum har file ko declare karte hain:
    - lib/assets/CLASHESQUAD.jpg
    - lib/assets/Freematch.jpg
    - lib/assets/Fullmap.jpg
    - lib/assets/lonewolf.jpg
    - lib/assets/survival.jpg
    - lib/assets/Weeklyspecial.jpg
    - lib/assets/notification_strip.png
    - lib/assets/withdrawal_strip.png
    - lib/assets/app_logo.png
    - lib/assets/opening_animation.mp4 # Video